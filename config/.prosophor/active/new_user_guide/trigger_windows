@echo off
rem 新用户引导触发脚本（Windows 版本）
rem 检测是否存在新用户标记文件
rem 返回非 0 = 触发（新用户），返回 0 = 不触发（老用户）

if exist "%USERPROFILE%\.prosophor\first_run_marker" (
    echo 检测到新用户标记文件
    exit /b 1
) else (
    exit /b 0
)
